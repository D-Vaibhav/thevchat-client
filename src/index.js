import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// the app component will be put inside the root and then rendered in ReactDOM
ReactDOM.render(<App />, document.querySelector('#root'));